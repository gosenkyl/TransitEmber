{{#if getRoutes.isRunning}}
  {{loading-widget text="LOADING ROUTES"}}
{{else}}
  {{#each routes as |route|}}
      {{route-widget route=route isMap=isMap}}
  {{else}}
    NO ROUTES
  {{/each}}
{{/if}}