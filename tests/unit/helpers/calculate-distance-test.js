
import { calculateDistance } from 'transit/helpers/calculate-distance';
import { module, test } from 'qunit';

module('Unit | Helper | calculate distance');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = calculateDistance([42]);
  assert.ok(result);
});

